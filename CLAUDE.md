# FlintMesh — Claude Instructions

## Git

- Never create branches, worktrees, or commits unless explicitly asked.
- The user handles all git operations. Only create a commit when directly requested.
