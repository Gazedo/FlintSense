[project]
name = "flint-hardware"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bd-warehouse>=0.2.0",
    "build123d>=0.10.0",
]
